export interface Client { 
    status?: number,
    access_token?: string,
    refresh_token?: string,
    user_id?: number,
    id_client?: number,
    nickname?: string,
    site_id?: string,
    expires_in?: Date,
    user?: number,
}